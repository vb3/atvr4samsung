"""Emulated Apple TV (Companion Link server) + mDNS advertisement.

``server.py`` subclasses the emulated Apple TV in ``protocol/``; ``discovery.py`` publishes the
``_companion-link._tcp`` record the iPhone discovers.
"""
